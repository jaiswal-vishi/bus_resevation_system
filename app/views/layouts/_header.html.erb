<header>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top shadow">
    <div class="container-fluid">
      <div class="collapse navbar-collapse" id="navbarExample01">
      	<strong>Bus Reservation System</strong>
				<% if current_user %>
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item">
	            <a class="nav-link" href="#">
							Hello! <%= current_user.role %></a>
						</li>
					</ul>
					<ul class="navbar-nav ">
						<%if current_user.bus_owner? && @show_link%>
    					<%= link_to "List Of your Buses", buses_path, :class => "btn btn-light"%>
    					<li class="nav-item", style="margin-left: 10px;">
								<%= link_to "Add Bus", new_bus_path, :class => "btn btn-primary"%>
							</li>
						<%end%>
						<%if current_user.bus_owner? && @home_show_link%>
							<li class="nav-item", style="margin-left: 10px;">
								<%= link_to "Add Bus", new_bus_path, :class => "btn btn-primary"%>
							</li>
							<li class="nav-item" style="margin-left: 10px;">
								<%= link_to "Home", root_path, method: :delete, :class => "btn btn-primary" %>
							</li>
						<%end%>
						<li class="nav-item" style="margin-left: 10px;">
							<%#= link_to "Sign_out", destroy_user_session_path, method: :delete, :class => "btn btn-danger"%>
							<%= link_to "Logout", logout_path, method: :delete, :class => "btn btn-danger" %>
						</li>
					</ul>
				<% else %>
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item">
            <a class="nav-link" href="#">Hey! You are not signed in yet.</a>
						</li>
					</ul>
					<ul class="navbar-nav ">
						<li class="nav-item">
							<%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-secondary"%>
						</li>
						<li class="nav-item" style="margin-left: 10px;">
							<%= link_to "Login", new_user_session_path, :class => "btn btn-primary" %>
						</li>
					</ul>
				<% end %>
			</div>
    </div>
  </nav>
  <!-- Navbar -->
</header>